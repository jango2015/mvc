define([
    'jquery',
    'backbone',
    'utils'
], function($, Backbone, utils){
	var FormModel = Backbone.Model.extend({
		
		initialize: function(){
			
		}
	});
	
	return new FormModel;
});